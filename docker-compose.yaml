version: '3'

services:
    gooutsafe:
        depends_on: [ 'redis' ]
        build: .
        ports:
            - "5000:5000"
        network_mode: "host"
        volumes:
        -   ./:/code
    redis:
        image: "redis:alpine"