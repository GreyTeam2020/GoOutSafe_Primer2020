[tox]
envlist = py36

[testenv]
passenv = TRAVIS TRAVIS_JOB_ID TRAVIS_BRANCH
deps = pytest
       pytest-cov
       flask_profiler
       coveralls
       -rrequirements.txt

commands =
    python -m pytest --cov-config .coveragerc --cov monolith
    - coveralls --ignore-errors